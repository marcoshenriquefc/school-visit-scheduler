# WordPress Integration

A API pública foi desenhada para consumo por plugin WordPress.

- Suporte opcional a `x-api-key` em endpoints públicos (`PUBLIC_API_KEY_REQUIRED=true`).
- Endpoints públicos retornam apenas dados necessários para renderização e envio de agendamentos.
